# Python f-string 格式化速查

## `:.1f` vs `:.1%` 对比（最易错）

| 写法 | 输入 | 输出 | 说明 |
|---|---|---|---|
| `:.1f` | `0.472` | `0.5` | 浮点，不做乘法 |
| `:.1%` | `0.472` | `47.2%` | 自动 ×100 + % 号 |

**核心**：百分比格式要求输入是**小数**（0.472 不是 47.2），否则会爆出 4720.0%。

---

## 百分比

| 写法 | 输入 | 输出 | 场景 |
|---|---|---|---|
| `f"{v:.0%}"` | `0.472` | `47%` | "了解率 47%" |
| `f"{v:.1%}"` | `0.472` | `47.2%` | 精确一位 |
| `f"{v:.2%}"` | `0.0356` | `3.56%` | 精确两位 |

---

## 浮点数

| 写法 | 输入 | 输出 | 场景 |
|---|---|---|---|
| `f"{v:.0f}"` | `3.36` | `3` | 取整 |
| `f"{v:.1f}"` | `3.36` | `3.4` | 一位小数 |
| `f"{v:.2f}"` | `3.3612` | `3.36` | 两位小数 |

---

## 整数

| 写法 | 输入 | 输出 | 场景 |
|---|---|---|---|
| `f"{v}"` | `320` | `320` | 普通 |
| `f"{v:,}"` | `32000` | `32,000` | 千分位 |
| `f"{v:05d}"` | `3` | `00003` | 编号补零 |

---

## 日期

```python
from datetime import date, datetime

f"{date.today()}"                        # '2026-01-15'
f"{date.today():%Y年%m月%d日}"           # '2026年01月15日'
f"{datetime.now():%Y-%m-%d %H:%M}"       # '2026-01-15 14:30'
f"{date.today().year}年{date.today().month}月"  # '2026年1月'
```

---

## 对齐

| 写法 | 效果 |
|---|---|
| `f"{'标题':^20}"` | 居中，宽 20 |
| `f"{'左对齐':<12}"` | 左对齐 |
| `f"{'右对齐':>10}"` | 右对齐 |

---

## 科学计数法

| 写法 | 输入 | 输出 |
|---|---|---|
| `f"{v:.2e}"` | `0.0000472` | `4.72e-05` |

---

## 报告常用句子模板

```python
# 样本量
f"本次共回收有效问卷 {N} 份。"

# 均值 + 城乡差距
f"大湾区认知度均值 {mean_val:.2f}，城镇 {urban:.2f}，农村 {rural:.2f}，差距 {diff:.2f} 分。"

# 百分点变化（delta 是百分点值，如 5.3，不是小数）
f"了解率 {pct:.0%}，较上期提升了 {delta:.1f} 个百分点。"

# 从比例推算百分点差
f"提升了 {(new_pct - old_pct)*100:.1f} 个百分点。"

# 比例
f"{cnt}/{total}（{cnt/total:.1%}）"

# 排名
f"位列第 {rank}，支持率 {pct:.0%}"

# 变化趋势
f"从大一 {freshman_rate:.0%} 升至研究生 {grad_rate:.0%}"

# 安全取值（防止 None 炸 format）
f"均值 {stats.get('mean', 0):.2f}"

# 报告日期
f"报告生成日期：{date.today():%Y年%m月%d日}"
```

---

## 易错点

| 错误 | 原因 | 正确 |
|---|---|---|
| `f"{p:.1%}"` 输出 `4720.0%` | p=47.2 不是 0.472 | 确保 p 是 0~1 的小数 |
| `f"{m:.2f}"` TypeError | m 是 None | `m if m is not None else 0` |
| `f"{v:,}"` 得到 `3,200.0` | v 是浮点 3200.0 | 先 `int(v)` |
| 百分点差用 `:.1%` | 差值变成 `0.5%` 而非 `5%` | 百分点差用 `:.1f` |
